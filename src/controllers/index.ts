const authControllers = require('./authController');
const userControllers = require('./userController');
const postControllers = require('./postController');
const categoryControllers = require('./categoryController');
const tagControllers = require('./tagController');


export const authController = authControllers;
export const userController = userControllers;
export const postController = postControllers;
export const categoryController = categoryControllers;
export const tagController = tagControllers;